<!DOCTYPE html>
<html lang="ja">

    <head>
        <meta http-equiv="content-type"
            content="text/html; charset=UTF-8">
        <title><%=title %></title>
        <link rel="stylesheet" href="/stylesheets/style.css" />
    </head>
    <body>
        <head>
            <h1><%=title %></h1>
        </head>
        <div role="main">
            <table>
                <% for(var i in content) { %>
                <tr>
                    <% var obj = content[i].attributes; %>      <!-- attributesプロパティに値が保存されている-->>
                    <th><%= obj.id %></th>
                    <th><%= obj.name %></th>
                    <th><%= obj.mail %></th>
                    <th><%= obj.age %></th>
                </tr>
                <% } %>
            </table>
        </div>
    </body>
</html>